/**
 * Headless bench runner. SPEC Section 19.1 determinism gate (3-run hash check).
 *
 * Phase 0 STUB — Playwright/Puppeteer wiring + scenario B replay land in Phase 6b.
 * Currently no-op so CI workflow can include this step.
 */
console.info('[bench-headless] STUB — full headless bench pipeline in Phase 6b.');
process.exit(0);
